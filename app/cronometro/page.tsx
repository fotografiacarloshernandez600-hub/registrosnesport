import type{Metadata}from"next";import RaceTimer from"./race-timer";import{getEvents}from"@/lib/events";
export const metadata:Metadata={title:"Cronómetro"};export const dynamic="force-dynamic";export default async function Page(){const events=await getEvents();return <main className="timer-page"><a className="brand logo-brand" href="/"><img src="/nesport-logo.png" alt="Nesport"/></a><div className="timer-shell"><span className="eyebrow"><span/> CRONÓMETRO DEL CORREDOR</span><h1>Tu carrera.<br/><em>Tu tiempo.</em></h1><p>Selecciona tu carrera, escribe el folio confirmado e inicia al cruzar la salida.</p><RaceTimer events={events.map(e=>({id:e.id,title:e.title}))}/></div></main>}
