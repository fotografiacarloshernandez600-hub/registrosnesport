import{notFound}from"next/navigation";import{CalendarDays,MapPin,Medal,Shirt,Sparkles,Trophy}from"lucide-react";import{getEvent}from"@/lib/events";import RegistrationForm from"../../registration-form";
export const dynamic="force-dynamic";
export default async function RaceDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params,event=await getEvent(slug);if(!event)notFound();const open=event.status==="published";const kit=[{image:event.shirt_image,icon:<Shirt/>,name:"Playera oficial"},{image:event.medal_image,icon:<Medal/>,name:"Medalla finisher"},{image:event.bib_image,icon:<span className="bib-mini">127</span>,name:"Dorsal personalizado"},{image:event.kit_image,icon:<Sparkles/>,name:"Kit del corredor"}].filter(x=>x.image||x.name);return <main><header className="nav-shell"><a href="/" className="brand logo-brand"><img src="/nesport-logo.png" alt="Nesport"/></a><nav><a href="/">Inicio</a><a href="/carreras">Carreras</a><a href="/cronometro">Cronómetro</a></nav><a className="admin-link" href="/dashboard">Portal administrador</a></header><section className="detail-hero" style={event.hero_image?{backgroundImage:`linear-gradient(90deg,#080a0de8,#080a0d88),url(${event.hero_image})`,backgroundSize:"cover",backgroundPosition:"center"}:undefined}><div><span>{open?"INSCRIPCIONES ABIERTAS":"PRÓXIMAMENTE"}</span><h1>{event.title}</h1><p>{event.description}</p><a className={`btn ${open?"primary":"ghost"}`} href={open?"#inscripcion":"#detalles"}>{open?"Inscribirme":"Conocer la carrera"}</a></div><aside><strong>{event.categories.split(",")[0].trim()}</strong><small>{event.location}</small></aside></section><section id="detalles" className="detail-facts"><div><CalendarDays/><small>FECHA</small><b>{new Date(event.event_date).toLocaleDateString("es-MX",{day:"2-digit",month:"long",year:"numeric",timeZone:"America/Mexico_City"})}</b></div><div><MapPin/><small>SALIDA Y META</small><b>{event.location}</b></div><div><Trophy/><small>CATEGORÍAS</small><b>{event.categories}</b></div><div><Sparkles/><small>INSCRIPCIÓN</small><b>$ {event.price} MXN</b></div></section><section className="race-story"><div><span>LA EXPERIENCIA</span><h2>Todo listo para cruzar la meta.</h2><p>{event.description}</p><h3>Tu inscripción incluye</h3><p>{event.includes}</p>{event.prizes&&<><h3>Premios</h3><p>{event.prizes}</p></>}</div><div className="kit-showcase media-kit">{kit.map(x=><article key={x.name}>{x.image?<img src={x.image} alt={x.name}/>:x.icon}<b>{x.name}</b></article>)}</div></section>{event.faq&&<section className="faq-section"><span>PREGUNTAS FRECUENTES</span><h2>Todo lo que necesitas saber</h2>{event.faq.split("\n").filter(Boolean).map((line,i)=>{const[q,a]=line.split("|");return <details key={i}><summary>{q}</summary><p>{a||"Consulta directamente con Nesport para más información."}</p></details>})}</section>}{open?<section id="inscripcion" className="registration-section"><div className="registration-intro"><span>INSCRIPCIÓN</span><h2>Asegura tu lugar</h2><p>Completa tus datos, revisa la responsiva y adjunta tu comprobante. Nesport validará el pago y asignará tu folio.</p><div className="price-box"><small>TOTAL A TRANSFERIR</small><strong>$ {event.price}</strong><span>MXN</span></div></div><RegistrationForm event={event}/></section>:<section className="coming-banner"><span>PRÓXIMAMENTE</span><h2>Las inscripciones aún no están abiertas.</h2><a className="btn dark" href="/carreras">Ver carreras disponibles</a></section>}</main>}
